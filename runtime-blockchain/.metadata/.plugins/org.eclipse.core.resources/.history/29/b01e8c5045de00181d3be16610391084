pragma solidity ^0.4.0;

contract [%=SmartContract.all.first().name%] {
 	[%for (ent in Entidad.all) { %]
		struct [%=ent.name%] {
		  [%for (attr in ent.atributos) { %]
			[%=attr.tipodato.name%] [%=attr.name%] 
		[%}%]
		}
[%}%] 
}
 
